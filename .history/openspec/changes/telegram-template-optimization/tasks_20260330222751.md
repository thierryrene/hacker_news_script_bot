# Tasks: Telegram Template Optimization

## Phase 1: Prompts and Layout Definition

- [ ] 1.1 Modificar `hn_summary.js` na função `summarizeAllWithGemini`: Reescrever o `prompt` da IA para exigir explicitly as seções de saída em "TL;DR" e "Insight", assegurando que ele pare de gerar um único parágrafo denso e passe a usar marcações semânticas claras.
- [ ] 1.2 No iterador principal de `hn_summary.js`, atualizar o template de concatenação (`fullMsg +=`) para adicionar a URL `🔗 <a href="https://news.ycombinator.com/item?id=${post.id}">Discussão (HN)</a>`.
- [ ] 1.3 Atualizar o loop em `hn_summary.js` para usar um delimitador fixo ao final de cada bloco de notícia (ex: `===POST_SEPARATOR===` ou quebras visuais garantidas, em vez de depender apenas de `\n\n`), reduzindo riscos do LLM quebrar o padrão.

## Phase 2: Payload Chunking Refactoring

- [ ] 2.1 Alterar a lógica de quebra de mensagem no `hn_summary.js`: `const blocks = fullMsg.split('\n\n');` deve ser atualizado para separar pelo novo delimitador adotado na Tarefa 1.3.
- [ ] 2.2 Ajustar a lógica que reconstrói os `chunks` a serem enviados à API do Telegram, garantindo que a união respeite as regras do Telegram (~4000 caracteres) sem cortar tags HTML abertas.

## Phase 3: Testing / Verification

- [ ] 3.1 Rodar testes de extração (pode usar logs para dry-run ou um bot de teste) e verificar se o limitador de chunk consegue tratar todas as respostas sem disparar `HTTP 400 - Bad Request` pela API do Telegram.
- [ ] 3.2 Validar o repasse da string para `evolution.sendMessage`. Verificar se as eventuais novas estruturas criadas (como o link do item no HN) convertem corretamente a formatação em `evolution.js` (Markdown fallback do WhatsApp).