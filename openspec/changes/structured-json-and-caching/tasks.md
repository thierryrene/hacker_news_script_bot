# Tasks: Structured JSON and Caching

- [x] 1. Implement SQLite caching via `cache_helper.py` (Python) and `cache.js` (Node.js).
- [x] 2. Refactor `hn_summary.js` to use concurrent scraping and comment fetching.
- [x] 3. Update `summarizeAllWithGemini` and `summarizeCommentsWithGemini` to use Gemini JSON Schema.
- [x] 4. Implement cache lookup and storage logic in the main `hn_summary.js` loop.
- [x] 5. Update `fullMsg` builder to include "Por Que Importa", Emojis, and Tags in the Telegram/WhatsApp templates.
- [x] 6. Update `script.js` to render the enriched data in the web viewer.
- [x] 7. Verify the full pipeline with a dry-run or test execution.
- [x] 8. Commit all changes and synchronize the workspace.
