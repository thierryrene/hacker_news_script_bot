# Tasks: Top Comments Summary

- [x] 1. In `hn_summary.js`, create `fetchTopComments(kids)` to grab the text of the first 3-5 comment IDs.
- [x] 2. Update `hn_summary.js` to perform a second Gemini call summarizing the community discussion.
- [x] 3. Append the community feedback into `exportedArray` so it natively pushes into `data/latest.json`.
- [x] 4. Send this separate payload to Telegram securely formatted via `commentsMsg`.
- [x] 5. Ensure `script.js` renders the community box below the post.
